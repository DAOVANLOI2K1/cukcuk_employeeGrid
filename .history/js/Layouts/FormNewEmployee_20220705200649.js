class FormNewEmployee {
    constructor(FormID) {
        let me = this;

        me.form = $(`#${}`);

        // Khởi tạo sự kiện
        me.initEvent();
    }

    // Hàm khởi tạo sự kiện
    initEvent() {

    }
}